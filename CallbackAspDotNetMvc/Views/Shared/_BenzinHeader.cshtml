<nav class="navbar navbar-full navbar-light bg-faded">
    <a class="navbar-brand" href="#">АРМ &laquo;Бензин&raquo;</a>
    <ul class="nav navbar-nav">
        <li class="nav-item active">
            <a class="nav-link" href="@Url.Action("Index", "Benzin")">Мои заправки <span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item active">
            <a class="nav-link" href="https://github.com/mgerasim/CallbackAspDotNet/issues">Добавить пожелание<span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item active">
            <a class="nav-link" href="@Url.Action("Control", "Benzin")">Контроль учёта <span class="sr-only">(current)</span></a>
        </li>
    </ul>
</nav>

@if (Request.Params["notice"] != "" && Request.Params["notice"] != null)
{
    <div class="alert alert-success alert-dismissible" role="alert">
        <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <strong>Успешно!</strong> @Request.Params["notice"]
    </div>
}

@if (Request.Params["error"] != "" && Request.Params["error"] != null)
{
    <div class="alert alert-danger alert-dismissible" role="alert">
        <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <strong>Ошибка!</strong> @Request.Params["error"]
    </div>
}